name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
      id-token: write  # for Codecov OIDC upload
    uses: iwamot/workflows/.github/workflows/validate-with-coverage.yml@6dd705693b7a451242519165c90446d73a6870c8 # v8.10.0
