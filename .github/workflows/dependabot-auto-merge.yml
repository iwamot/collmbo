name: Dependabot auto-merge
on: pull_request

permissions: {}

jobs:
  dependabot-auto-merge:
    permissions:
      contents: write       # to merge auto-merge PRs
      pull-requests: write  # to enable auto-merge
    uses: iwamot/workflows/.github/workflows/dependabot-auto-merge.yml@ae5512be479f765d6f3f89302516a6f5c0add845 # v4.1.3
