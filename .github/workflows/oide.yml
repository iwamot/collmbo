name: oide

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/oide.yml

permissions: {}

jobs:
  pull:
    permissions:
      contents: write       # to push the branch
      pull-requests: write  # to open the PR
    uses: iwamot/workflows/.github/workflows/oide.yml@d4659e508a3f159783aa5295d3da79047db0a940 # v3.1.0
