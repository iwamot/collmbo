name: 'Auto label by title'
on:
  pull_request:
    types: [opened]
    branches: [ "main" ]

permissions: {}

jobs:
  label:
    permissions:
      pull-requests: write  # to add a label to the PR
    uses: iwamot/workflows/.github/workflows/auto-label.yml@fe9df89e74648e67a0cda3f708718e9f11db2ce7 # v8.0.2
