name: 'Auto label by title'
on:
  pull_request:
    types: [opened]
    branches: [ "main" ]

permissions: {}

jobs:
  label:
    permissions:
      pull-requests: write  # to add a label to the PR
    uses: iwamot/workflows/.github/workflows/auto-label.yml@e1011cc73c85c693d3f26dc243c7cc26e66aa67b # v8.5.0
