name: 'Auto label by title'
on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  label:
    permissions:
      pull-requests: write  # to add a label to the PR
    uses: iwamot/workflows/.github/workflows/auto-label.yml@cdc42cf2f91fd796de35bdc27aaaa1057695afcc # v6.1.0
