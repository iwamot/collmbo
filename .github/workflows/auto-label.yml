name: 'Auto label by title'
on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  label:
    permissions:
      pull-requests: write  # to add a label to the PR
    uses: iwamot/workflows/.github/workflows/auto-label.yml@ebf1be0e9a042e490eef4062613562225d4d206f # v5.0.0
