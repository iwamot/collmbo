name: 'Auto label by title'
on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  label:
    permissions:
      pull-requests: write  # to add a label to the PR
    uses: iwamot/workflows/.github/workflows/auto-label.yml@15e638a17daad208034700f2429ee13db2452ce4 # v4.2.0
